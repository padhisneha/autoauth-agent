# AutoAuth Agent — Backend Environment Variables
# Copy this file to .env and fill in your values

# OpenAI API key — used by ClinicalReaderAgent, PolicyAgent, and AppealAgent
OPENAI_API_KEY=your_openai_api_key_here

# Set to true to skip LLM calls and use rule-based fallback logic (no API key needed)
DEMO_MODE=false
